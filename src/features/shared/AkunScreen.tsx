import { Placeholder } from '@/components/layout/Placeholder';

/** Porting dari `features/shared/AkunIdentitasForm.tsx` — lihat docs/MAPPING.md §6. */
export function AkunScreen() {
  return (
    <Placeholder title="Akun saya" icon="settings" source="features/shared/AkunIdentitasForm.tsx" />
  );
}
