import { Placeholder } from '@/components/layout/Placeholder';

/** Porting dari `components/documents/DocumentViewer.tsx` — lihat docs/MAPPING.md §6. */
export function DocumentViewer() {
  return (
    <Placeholder
      title="Berkas KTP"
      icon="shield"
      source="components/documents/DocumentViewer.tsx"
    />
  );
}
